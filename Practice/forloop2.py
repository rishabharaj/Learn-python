numbers = [1, 2, 3, 4, 5, "tamatar"]
for num in numbers:
    print(num)

# This loop iterates over each element in the numbers list.
# On each iteration, the variable num takes the value of the next item in the list.
