#  Python can be passed arbitrary code as a string in the shell:
#   python -c 'print("Hello, World")'
#  Hello, World
#  This can be useful when concatenating the results of scripts together in the shell. 
# this will work in cmd mode
